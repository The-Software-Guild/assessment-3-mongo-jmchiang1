import React from "react";

const Home = () => (
  <div style={{display: 'flex', justifyContent: "center", margin: '15rem'}}>
    <h1>Welcome to my bug tracker!</h1>
  </div>
);

export default Home;
